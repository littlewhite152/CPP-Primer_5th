# 数组
与vector不同的地方：数组的大小固定，不能随意向数组中增加元素，其他用法和`vector`类型差不多<br/>
必须由**常量表达式**来定义数组大小<br/>
用字符串常量初始化时要注意结尾的'\0'也会拷贝到数组中去
```c
char a[] = "c++"//此时a的数组长度为4，分别为'c','+','+','\0'
```
数组下标是`size_t`类型,是一种机器相关的**无符号**类型，设计得很大