#include<iostream>
int main(){
    int v1 = 1, v2 = 1;
    std::cout << "the sum of " << v1;
             <<" and " << v2;//前面应该加上std::cout
             <<" is " << v1 + v2 << std:endl;//前面应该加上std::cout
}